# Development / CI dependencies. No vendor firmware is needed to run the tests —
# they build a synthetic package from scratch (see tests/helpers.py).
pytest>=8
ruff>=0.16
# tools/ppcemu.py — the function-level PowerPC emulator and its tests
unicorn>=2.0
capstone>=5.0
